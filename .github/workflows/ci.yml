name: Kirov CI Pipeline

on:
  push:
      branches: [main, dev]
        pull_request:
            branches: [main, dev]

            jobs:
              # BACKEND (Python / FastAPI)
                backend:
                    name: Backend 
